class 14 HashMap Implementation Arrays, Collections
1, .equals is the equal in semantic level.
2, == , for primitive, it compares the values, for objects, it compares the address.  

steps to implement a Class
1, Class structure, API
2, Data -> Fields
3, Constructor: capacity, load factor(threshold), no parameter
4, Implementation

what is View ?

entrySet() 

In Java 8, use getOrDefault(Object key, V defaultValue)

HashSet 
add : return true if this set did not already contain the specified element


Remove when traverse
//ConcurrentModificationException


